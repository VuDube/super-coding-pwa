Frontend dev notes:
- To run frontend locally and proxy API calls to backend:
  1. cd web-frontend
  2. npm install
  3. npm run dev
- If backend is at http://localhost:8000, configure Vite proxy in vite.config.js or run both in environments where /api is proxied to backend.
